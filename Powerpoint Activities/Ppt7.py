#Program to ask user for their name

#list = ['a','e','i','o','u']
#def main():
    #name = input("Enter your name:").lower()
    #count = 0
    #for char in name:
        #if char in list:
            #count += 1
    #print("Out of",len(name),"letters,",name,"has a total of",count,"vowels")

#main()

#Write list comprehension that produces a list of the words that have > 3 characters (ask later for one line comprehension)
#list = []
#def main():
    #text = "This is a sentence"
    #divided = text.split()
    #for char in divided:
        #if len(char) > 3:
            #list.append(char)
    #print(list)
#main()

